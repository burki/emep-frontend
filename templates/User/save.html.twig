{% extends 'Base/base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-sm-10">
            <div class="row">
                <div class="col-sm-2"></div>
                <div class="col-sm-10" style="margin-bottom: 25px;"><strong>Set a descriptive title for your query:</strong></div>
            </div>

            {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
            {# renders all fields *and* the form start and end tags #}
            {{ form(form) }}
        </div>

    </div>

{% endblock %}