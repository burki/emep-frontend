{% block tabsHeader %}
    {% import 'Shared/helper.html.twig' as helper %}
    <!-- Nav tabs -->
    <div>
        <div class="tabsymbols">
            <div class="advanced-search-settings"><span class="checkbox-filter-button">apply checkbox filter</span></div>
            <div class="icontab download-csv"><a href="{{ path('search-export', { 'entity' : 'Holder' }|merge(app.request.query.all)) }}"><img src="{{ app.request.basepath }}/img/download_icon_blue.svg" class="iconimage" title="download current data"></a></div>

            {{ helper.show_savedsearches(searches) }}
        </div>

        <ul class="nav nav-tabs" role="tablist" style="margin-bottom: 1em">
            <li role="presentation" class="active"><a href="#exhibitions" aria-controls="entries" role="tab" data-toggle="tab">{{ 'Holding Institutions'|trans }}</a></li>
        </ul>
    </div>

    <script>
        function clickMap() {
            $(document).trigger('rerenderMap');
        }
    </script>
{% endblock %}